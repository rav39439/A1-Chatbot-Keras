You are asking specifically about models that support LoRA (Low-Rank Adaptation) training for image generation / image-to-image translation.

Below is the complete, accurate list.

✅ **All pretrained transformer-based models that support LoRA training

(for image generation + image translation)**

There are 6 major families and 27+ specific models that support LoRA.

🧩 FAMILY 1 — Diffusion Transformers (DiT family)

(LoRA is natively supported)

These are state-of-the-art for image generation & translation.

✔ Models:

Stable Diffusion 1.x (UNet transformer blocks → LoRA)

Stable Diffusion 2.x

Stable Diffusion XL (SDXL)

Stable Cascade (V1 & V2)

Kandinsky 2.1 / 2.2

PixArt-α

PixArt-Σ (Sigma)

OpenDiT (Meta's DiT)

Flux / Flux.1 (fully transformer generator → supports LoRA)

Hunyuan-DiT (Tencent)

AuraFlow (100% DiT backbone)

Sora-like Video Transformers
 → used for video-to-video translation via LoRA

Tasks supported

✔ image generation
✔ image-to-image
✔ style transfer
✔ texture transfer
✔ face swapping
✔ object replacement
✔ style LoRAs / dreambooth LoRAs
✔ video LoRAs (for Sora-style models)

🧩 FAMILY 2 — Vision Transformers (ViT family)

Fine-tunable with LoRA for:

image translation

colorization

inpainting

super-resolution

masked autoencoding

✔ Models:

ViT-B/L/H (Google Vision Transformers)

MAE / Masked Autoencoder ViT

iBOT

SimMIM

DINOv2

Swin Transformer

SegFormer (MiT Transformers)

Tasks supported

✔ image translation
✔ segmentation-to-image
✔ domain transfer
✔ enhancement
✔ restoration

🧩 FAMILY 3 — VQ / Token Transformers

These are token-based generators — perfect for image generation and translation.

✔ Models:

VQGAN + Transformer

DALL·E (VQ-VAE + GPT Transformer)

DALL·E mini / mega

VQ-VAE-2

VQ-Diffusion

VQGAN-CLIP transformers

These support LoRA because the transformer part is LoRA-compatible.

🧩 FAMILY 4 — Autoregressive Image Transformers

These are pixel/token autoregressive transformers.

✔ Models:

ImageGPT

PixelSNAIL Transformer

Perceiver-AR

Tasks supported

✔ unconditional generation
✔ conditional translation
✔ patch-level translation
✔ token-to-token mapping

🧩 FAMILY 5 — Multimodal Transformers (image→image with text conditioning)

These support LoRA and can be used for conditional translation.

✔ Models:

BLIP-2 + ViT

PaLI-GEMMA (Google)

Flamingo / DeepFloyd

Chameleon (Meta)

Kosmos-2

These can translate with instruction prompts.

🧩 FAMILY 6 — Video Transformers (image→image translation extended to video)

These also support LoRA:

Video DiT

AnimateDiff (LoRA-heavy)

VideoCrafter

🎯 TOTAL count

👉 36 major pretrained transformer models support LoRA for image generation and image translation.

This includes:

pure transformers (ViT / DiT)

hybrid UNet+transformer diffusion models

autoregressive token transformers

VQ token transformers

multimodal transformers

video transformers

📌 If you want, I can now generate:
✔ A list of the best models for your exact translation task
✔ A comparison: “Which transformer is best for image translation?”
✔ A full LoRA training script for any of the models above
✔ A LoRA training pipeline for

SDXL

DiT

ViT autoencoder

VQGAN+Transformer

PixArt-Σ

✔ Compute requirement recommendations

Just tell me:

👉 What is your image translation task? (e.g., A→B: sketch→photo, day→night, style→style, etc.)


Implementations:

https://www.kaggle.com/code/abdelrahmanmahmoud11/vit-b-16
